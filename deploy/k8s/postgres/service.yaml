apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: shopkuber
spec:
  clusterIP: None   # Headless service for StatefulSet
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
